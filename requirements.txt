fastapi
uvicorn
pydantic
requests
neo4j
python-dotenv
# Required by FastAPI to parse multipart form data — the dashboard's SRS/Figma
# upload (POST /targets/upload-knowledge) fails at import time without it.
python-multipart
droidrun
logtail-python

# ── Web player (web_player/) ────────────────────────────────────────────────
# Browser driver for the website executor. After installing, fetch the browser
# binaries once:   playwright install chromium
playwright
# Lets the run lock spot a batch that never took a lock - one started
# before the lock existed, or launched by a script bypassing the runner.
# Optional: without it the lock file alone guards concurrency.
psutil

# ── Smart ingestion ─────────────────────────────────────────────────────────
# Format-agnostic document loading (PDF/DOCX/PPTX/HTML/MD/...).
markitdown
pypdf
# Semantic (vector) retrieval embeddings — ONNX, no torch.
fastembed
numpy

# Optional upgrades (not required; the loader/embedder auto-detect them):
#   docling                 # SOTA layout-aware PDF/Office parsing
#   unstructured            # robust fallback for messy scans
#   sentence-transformers   # alternative (torch-based) embedding backend

# ── Reporting ───────────────────────────────────────────────────────────────
# PDF generation for the downloadable run report (GET /report/run.pdf) and for
# scripts/make_user_guide.py. Pure Python — no system libraries to install.
fpdf2

# ── Agent Framework ─────────────────────────────────────────────────────────
langchain
langchain-core
langgraph
structlog
llama-index-llms-openrouter
